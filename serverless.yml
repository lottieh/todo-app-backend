service: todo-app-backend

provider:
  name: aws
  runtime: nodejs12.x

  region: eu-west-2

functions:
  hello:
    handler: handler.tasks
    events:
     - http:
         path: tasks
         method: get
         cors: true

